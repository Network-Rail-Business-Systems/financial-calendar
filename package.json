{
  "type": "module",
  "scripts": {
    "phpstan": "vendor/bin/phpstan analyse -c phpstan.neon",
    "pint": "vendor/bin/pint --preset=per",
    "prepare": "husky",
    "test": "vendor/bin/phpunit"
  },
  "dependencies": {
    "husky": "^8"
  }
}
